import React from 'react'

const Header = () => (
  <header className="uk-margin-large uk-padding-large uk-background-primary">
    <h1 className="uk-text-center uk-padding-large">
      Event Finder with EventBrite API
    </h1>
  </header>
)

export default Header
